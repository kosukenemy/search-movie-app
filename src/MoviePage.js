import React from 'react';
import {withRouter} from "react-router-dom";

const MoviePage = () => {
    return(
        <h2>
            遷移先のページです。
        </h2>
    )
}
export default withRouter(MoviePage);